{
  "name": "@backero/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript types/Zod schemas shared between the backend API contract and the mobile/web clients. Populated incrementally as each domain module ships (Phase 2+) — empty by design in Phase 1.",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "zod": "^3.23.8"
  }
}
